# Postgres 연결 문자열
DATABASE_URL="postgresql://user:password@localhost:5432/builbook?schema=public"

# Auth.js 세션 암호화 시크릿 (npx auth secret 로 생성)
AUTH_SECRET="change-me"

# (선택) OAuth 프로바이더 — 필요 시 추가
# AUTH_GITHUB_ID=
# AUTH_GITHUB_SECRET=
