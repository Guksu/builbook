# 루프: 스크리브너 참고 UI/UX 개선 루프

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-09-15 |
| 작성 | Claude |
| 상태 | 성공 종료 |
| 실행 수단 | 세션 내 단계 반복 + 검증자 게이트(Stop 훅) |

## 1. 목표

사용자가 고른 4항목(스크리브닝 연속 보기 / 라벨·상태 색 + 문서 메모 / 목표 바 + 빠른 열기 / 문서 템플릿)이 전부 구현되고, 각 항목 종료 시 `npm run lint` + `npx tsc --noEmit` + `npm test` + `npm run build` + `npx playwright test` 전체 통과. 통과 즉시 main에 푸시.

## 2. 루프 설계 — 사용자 확인: 2026-09-15 확인됨(항목 선택 문답)

| 요소 | 값 |
|------|-----|
| 트리거 | 사용자 요청 1회 ("편의성과 UI 개선, 스크리브너 참고, 루프 진행") + 선행 수정(폴더 안 문서 최상위 이동, fee7ea4) |
| 실행 단위 | 항목 1개 = 1반복. 구현 → 검증 → 커밋·main 푸시 → 실행 기록 갱신 |
| 검증자 | 기계적 검증만: lint · tsc · vitest · next build · playwright |
| 종료 규칙 | 성공: 4항목 완료 + 검증 전체 통과. 실패: 안전장치 도달 시 중단·보고 |

## 3. 안전장치

| 장치 | 값 |
|------|-----|
| 최대 반복 | 10회 |
| 토큰 예산 | 2,000,000 토큰 — 초과 시 중단·보고 |
| 막힘 판정 | 같은 실패 시그니처 3연속 (verifierGate.config.json stuckAfter: 3) |

## 4. 실행 기록

| 반복 | 결과 | 비고 |
|------|------|------|
| 1 | 통과 — 스크리브닝 연속 보기: 폴더 클릭/Enter=선택, `widgets/scrivenings`(구획별 Tiptap+자동저장, "이 문서만 열기"), `collectDescendantDocs`(테스트 8) | e2e scrivenings 4 |
| 2 | 통과 — 라벨·상태·메모: `Project.labels`(기본 4개, 팔레트 8색), `DocumentNode.label/note`, 인스펙터 상태·라벨·라벨 관리·메모, 바인더 라벨 막대·상태 점, 코르크보드 색 띠 | unit labels 14 · e2e labels 3 |
| 3 | 통과 — 목표 바 + 빠른 열기: 헤더 `GoalBar`(작품·오늘 진행, 마감 페이스), `Project.deadline`+`paceToDeadline`(테스트 8), `features/quick-open`(Ctrl/⌘+P, 제목 점수 정렬, 테스트 7) | e2e 2 |
| 4 | 통과 — 문서 템플릿: `DocumentNode.kind`, 인물/설정 카드 본문 템플릿(테스트 5), 바인더 "카드 템플릿" 버튼·메뉴, 회차 분량표·내보내기(TXT/MD/DOCX)에서 카드 제외, 행에 종류 표시 | e2e 1 |
| — | 통합 검증 | lint 0 · tsc 0 · unit 308 · build OK(작업실 246KB) · e2e 59/59 |
| 0 | 선행 수정 통과 — 폴더 안 문서를 최상위로: 메뉴('한 단계 위로'/'최상위로'), 폴더 위쪽 1/3 드롭=앞, 트리 아래 빈 공간 드롭=최상위 | unit 268 · e2e 49/49 |

## 5. 종료 보고

**성공 종료 (2026-09-15).** 4항목 전부 구현·검증, main 푸시. 안전장치 미발동(반복 4/10, 막힘 0). 중간에 e2e 27건이 한꺼번에 깨진 원인은 새 버튼 이름("템플릿으로 새 문서")이 기존 "새 문서" 버튼 검색과 겹친 것 — 이름을 "카드 템플릿"으로 바꿔 해결.

- 남은 실패: 없음.
- 후속 후보: 라벨로 바인더·코르크보드 필터, 정렬 기준 영속화, 스크리브닝에서 폴더 선택 시 코르크보드도 그 폴더만 보기, 바인더 다중 선택, 컴파일 옵션(회차 범위·구분선).
- 상세: `docs/worklogs/2026-09-15-startup-polish.md` §2 "루프 2".
