# 루프: 스크리브너 참고 UI/UX 루프 2 (필터·범위·다중 선택·컴파일)

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-09-15 |
| 작성 | Claude |
| 상태 | 성공 종료 |
| 실행 수단 | 세션 내 단계 반복 + 검증자 게이트(Stop 훅) |

## 1. 목표

앞 루프의 후속 후보 4항목(바인더 다중 선택 / 라벨 필터(바인더·코르크보드) / 폴더 범위 코르크보드 / 컴파일 옵션)이 구현되고 lint · tsc · vitest · build · playwright 전체 통과. 통과 즉시 main 푸시.

## 2. 루프 설계 — 사용자 확인: 2026-09-15 확인됨("다음 진행해" — 앞 보고의 후속 후보 그대로)

| 요소 | 값 |
|------|-----|
| 트리거 | 사용자 요청 1회 |
| 실행 단위 | 항목 묶음 = 1반복. 구현 → 검증 → 커밋·main 푸시 → 기록 |
| 검증자 | 기계적 검증만: lint · tsc · vitest · next build · playwright |
| 종료 규칙 | 성공: 4항목 완료 + 검증 통과. 실패: 안전장치 도달 시 중단·보고 |

## 3. 안전장치

| 장치 | 값 |
|------|-----|
| 최대 반복 | 10회 |
| 토큰 예산 | 2,000,000 토큰 |
| 막힘 판정 | 같은 실패 시그니처 3연속 (verifierGate.config.json stuckAfter: 3) |

## 4. 실행 기록

| 반복 | 결과 | 비고 |
|------|------|------|
| 1 | 통과 — 바인더 다중 선택(Ctrl/⌘·Shift 클릭, 선택 바, 일괄 휴지통·최상위·한 단계 위, 다중 드래그) + 바인더 라벨 필터(조상 유지·자동 펼침). 순수: `selectRange`·`dropDescendants`·`filterTreeByLabel`(테스트 13). `deleteDocuments`(한 트랜잭션) | e2e binder-multi 3 |
| 2 | 통과 — 코르크보드 폴더 범위(`buildCards(docs, rootId)`), 라벨 필터(`filterCardsByLabel`), 툴바(범위·전체 보기·라벨 select). 테스트 8 | e2e 1 |
| 3 | 통과 — 컴파일 옵션: `compileManuscript`(회차 범위·구분선·회차/폴더/작품 제목 포함, 테스트 7)를 TXT·MD·DOCX가 공통 소비. 내보내기 모달에 옵션 UI | e2e 1(TXT 내용 검증) |
| — | 통합 검증. 빠른 열기 Esc가 포커스 타이밍에 따라 안 닫히던 흔들림 → window 리스너로 고정 | lint 0 · tsc 0 · unit 333 · build OK(249KB) · e2e 64/64 |

## 5. 종료 보고

**성공 종료 (2026-09-15).** 4항목 구현·검증, main 푸시. 안전장치 미발동(반복 3/10).

- 남은 실패: 없음.
- 지시와 다르게 한 것(에이전트 보고, 타당하여 수용): 여러 항목을 문서 위에 끌어다 놓으면 "그 줄 앞"이 아니라 "그 부모의 맨 끝"으로 모인다(루프 호출 시 stale 목록 문제 회피). 하나만 끌면 기존처럼 정확한 위치.
- 후속 후보: 정렬 기준 영속화, 라벨 필터 영속화, 코르크보드 카드 크기 조절, 컴파일 프리셋 저장.
