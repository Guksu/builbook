# 루프: 작업실 냄새 정리 루프 (선택 복원·이동 롤백·탭 접근성·훅 안정화)

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-09-17 |
| 작성 | Claude |
| 상태 | 성공 종료 |
| 실행 수단 | 세션 내 단계 반복 + 검증자 게이트(Stop 훅) |

## 1. 목표

앞 루프 리팩터링 에이전트가 보고한 냄새 4건 + 사용자 추가 요청(헤더 옵션이 많아 가시성이 나쁨 → 정비)(선택 복원 이펙트 2패스 / 여러 개 이동 부분 실패 시 롤백 없음 / 인스펙터 탭 `aria-controls`·`tabpanel` 없음 / `useDocuments`가 매 렌더 새 함수 반환)이 고쳐지고 lint · tsc · vitest · build · playwright 전체 통과, CI 성공. 통과 즉시 main 푸시.

## 2. 루프 설계 — 사용자 확인: 2026-09-17 확인됨("다음 후보 모두 진행해줘")

| 요소 | 값 |
|------|-----|
| 트리거 | 사용자 요청 1회 |
| 실행 단위 | 4건 = 1반복. 구현 → 검증 → 커밋·main 푸시 → 기록 |
| 검증자 | 기계적 검증만: lint · tsc · vitest · next build · playwright · GitHub Actions |
| 종료 규칙 | 성공: 4건 완료 + 검증 통과. 실패: 안전장치 도달 시 중단·보고 |

## 3. 안전장치

| 장치 | 값 |
|------|-----|
| 최대 반복 | 10회 |
| 토큰 예산 | 2,000,000 토큰 |
| 막힘 판정 | 같은 실패 시그니처 3연속 |

## 4. 실행 기록

| 반복 | 결과 | 비고 |
|------|------|------|
| 1 | 통과 — ① 선택 복원을 한 패스로(사라짐→재선택 사이 빈 렌더 제거) ② 여러 개 이동 중 실패 시 앞서 옮긴 것 되돌리기 + 안내 ③ 인스펙터 탭 `id/aria-controls`·`role=tabpanel` ④ `useDocuments` 액션을 `useMemo([projectId, mutate])`로 고정, 액션 안에서는 SWR 캐시 대신 DB를 신선하게 읽음 ⑤ (추가 요청) 헤더 정비: 칩 7개 → "패널" 메뉴(열린 수 배지) + 인스펙터 + "더 보기"(미리보기·내보내기·테마) + 집중. ContextMenu에 체크박스·비활성 항목 추가. e2e 12개 스펙의 칩 클릭을 메뉴 경유로 갱신 | lint 0 · tsc 0 · unit 344 · build OK(254KB) · e2e 72/72 |

## 5. 종료 보고

**성공 종료 (2026-09-17).** 냄새 4건 + 헤더 정비 구현·검증, main 푸시. 안전장치 미발동(반복 1/10).

- 헤더는 스크린샷으로 확인: 우측이 [본문|카드] [패널 1 ▾] [인스펙터] [⋯] [집중] 다섯 묶음으로 줄었다.
- 트레이드오프: 연표·현황·점검·검색·리서치·휴지통은 한 번 더 눌러야 한다(메뉴 경유). 자주 쓰는 패널이 생기면 인스펙터처럼 밖으로 꺼내는 것을 다음 후보로.
- 후속 후보: 패널 메뉴 단축키(예: Ctrl/⌘+1~6), 열린 패널 상태 작품별 기억.
